from pages.home_page import HomePage


class TestMenu:
    def test_menu_elements(self, driver):
        base_page = HomePage(driver)
        base_page.open()

    def test_menu_forms(self, driver):
        base_page = HomePage(driver)
        base_page.open()

    def test_menu_alerts(self, driver):
        base_page = HomePage(driver)
        base_page.open()

    def test_menu_widgets(self, driver):
        base_page = HomePage(driver)
        base_page.open()

    def test_menu_interactions(self, driver):
        base_page = HomePage(driver)
        base_page.open()

    def test_menu_book(self, driver):
        base_page = HomePage(driver)
        base_page.open()

